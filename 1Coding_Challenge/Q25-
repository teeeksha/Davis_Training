#Question 25: Create a function to calculate simple interest.
#Input: Enter P, R, T
#Output: Simple Interest

class Interest:
    def __init__(self, p, r, t):
        self.p = p
        self.r = r
        self.t = t

    def calculate(self):
        return (self.p * self.r * self.t) / 100

p = float(input("Enter P: "))
r = float(input("Enter R: "))
t = float(input("Enter T: "))

obj = Interest(p, r, t)
print("SI:", obj.calculate())
     #Output:-
Enter P: 2300
Enter R: 12
Enter T: 2
SI: 552.0
