# Example Input
marks = {"A":80, "B":95, "C":78}

# Assume first student as highest
top_student = ""
max_marks = 0

# Find highest marks
for student in marks:
    if marks[student] > max_marks:
        max_marks = marks[student]
        top_student = student

print(top_student)
#Output:-B
