#Question 15: Tuple to Dictionary Mapping
#Problem Statement:Convert two tuples into a dictionary where one tuple contains keys and the other contains values.
keys = ('a', 'b', 'c')
values = (1, 2, 3)

result = dict(zip(keys, values))

print(result)

     
{'a': 1, 'b': 2, 'c': 3}
